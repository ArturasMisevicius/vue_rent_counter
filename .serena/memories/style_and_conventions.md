Follow AGENTS.md strictly. Use Eloquent models/scopes/relations, avoid raw SQL, avoid queries in Blade and loops, avoid Model::all() without scope/limit/pagination, preload relations used by Filament columns/views. Filament is foundation namespace for actions/support in this repo. Keep controllers thin, use Form Requests, Policies, action classes, and explicit authorization. For code edits prefer focused minimal changes, ASCII, and preserve existing patterns. For code-task responses structure final answer as: PROBLEM, SOLUTION, QUERY DELTA, REUSABLE SNIPPET, BLADE USAGE, FILAMENT INTEGRATION, TESTS, CAVEATS.