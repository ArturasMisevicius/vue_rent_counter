Common commands for this Darwin-based repo: composer install; cp .env.example .env; php artisan key:generate; touch database/database.sqlite; php artisan migrate; npm install; npm run build; composer run setup; composer run dev; php artisan serve; php artisan queue:listen --tries=1 --timeout=0; npm run dev; php artisan test --compact; php artisan test --stop-on-failure; vendor/bin/pint --dirty; git status --short; git diff --check; rg for fast search. Use focused Pest runs for changed behavior before broader runs.