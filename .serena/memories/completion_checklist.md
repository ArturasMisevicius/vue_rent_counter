Before claiming completion in this repo: run fresh targeted tests for the changed behavior, run vendor/bin/pint --dirty, run git diff --check, and report exact command outputs. If behavior is query-sensitive, verify query shape/count implications from code paths. Do not claim success without fresh command evidence. Respect AGENTS.md pre-commit checks, especially no queries in Blade/loops, eager loading of view relations, named/grouped routes, and validation via Form Requests.