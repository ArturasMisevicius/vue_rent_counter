---
phase: 01-safety-freeze-and-guardrails
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - routes/web.php
  - app/Providers/AppServiceProvider.php
  - app/Http/Controllers/CspViolationReportController.php
  - app/Models/SecurityViolation.php
  - routes/console.php
  - tests/Feature/Security/SecurityHeadersTest.php
  - tests/Feature/Security/CspReportRateLimitTest.php
autonomous: true
requirements:
  - SEC-05
must_haves:
  truths:
    - "The public `/csp/report` endpoint stays reachable for valid CSP reports."
    - "Repeated CSP reports from one IP are throttled with `429` after the configured burst."
    - "Accepted CSP telemetry is tagged and prunable after the retention window without deleting unrelated security records."
  artifacts:
    - path: "app/Providers/AppServiceProvider.php"
      provides: "Named `security-csp-report` rate limiter"
      contains: "RateLimiter::for('security-csp-report'"
    - path: "app/Models/SecurityViolation.php"
      provides: "Tagged CSP pruning rule"
      contains: "MassPrunable"
    - path: "tests/Feature/Security/CspReportRateLimitTest.php"
      provides: "Executable proof for throttle, source tagging, and prune targeting"
      contains: "assertTooManyRequests"
  key_links:
    - from: "routes/web.php"
      to: "app/Providers/AppServiceProvider.php"
      via: "throttle:security-csp-report middleware"
      pattern: "security-csp-report"
    - from: "app/Http/Controllers/CspViolationReportController.php"
      to: "app/Models/SecurityViolation.php"
      via: "metadata source tag used by pruning"
      pattern: "source"
---

<objective>
Keep the public CSP intake endpoint functional while making it bounded, source-tagged, and self-cleaning.

Purpose: Preserve security telemetry without leaving `/csp/report` as an effectively unbounded public write sink.
Output: A named CSP rate limiter, tagged security-violation telemetry, a pruning rule, and tests that prove the behavior.
</objective>

<execution_context>
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
@.planning/phases/01-safety-freeze-and-guardrails/01-RESEARCH.md
@routes/web.php
@app/Providers/AppServiceProvider.php
@app/Http/Controllers/CspViolationReportController.php
@app/Services/Security/SecurityMonitor.php
@app/Models/SecurityViolation.php
@routes/console.php
@tests/Feature/Security/SecurityHeadersTest.php

<interfaces>
From app/Http/Controllers/CspViolationReportController.php:
```php
public function __invoke(
    CspViolationRequest $request,
    SecurityMonitor $securityMonitor,
): Response
```

From app/Services/Security/SecurityMonitor.php:
```php
public function recordViolation(
    Request $request,
    SecurityViolationType $type,
    SecurityViolationSeverity $severity,
    string $summary,
    array $metadata = [],
): SecurityViolation
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CSP throttle and retention regression tests, then implement the hardening</name>
  <files>routes/web.php, app/Providers/AppServiceProvider.php, app/Http/Controllers/CspViolationReportController.php, app/Models/SecurityViolation.php, routes/console.php, tests/Feature/Security/SecurityHeadersTest.php, tests/Feature/Security/CspReportRateLimitTest.php</files>
  <read_first>
    - tests/Feature/Security/SecurityHeadersTest.php
    - app/Http/Controllers/CspViolationReportController.php
    - app/Services/Security/SecurityMonitor.php
    - app/Models/SecurityViolation.php
    - app/Providers/AppServiceProvider.php
    - routes/web.php
    - routes/console.php
    - .planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Ten valid `POST /csp/report` requests from the same IP within one minute return `202`, and the eleventh returns `429`.
    - Test 2: Accepted CSP reports still dispatch `SecurityViolationDetected`, still create a `SecurityViolation`, and now persist `metadata['source'] = 'csp-report'`.
    - Test 3: The pruning rule selects old `csp-report` rows and leaves unrelated `SecurityViolation` records untouched.
  </behavior>
  <action>Start with the red step by keeping `tests/Feature/Security/SecurityHeadersTest.php` as the acceptance-flow proof and extending its metadata expectation to include `source => 'csp-report'`, then add `tests/Feature/Security/CspReportRateLimitTest.php` with a concrete throttle scenario that submits eleven valid CSP report payloads from the same IP and expects `429` on the eleventh request. In that same file, add a pruning-targeting assertion that creates old `SecurityViolation` rows with `metadata['source'] = 'csp-report'`, recent CSP rows, and unrelated non-CSP rows, then asserts the model's `prunable()` query only selects the old CSP rows. Complete the green step in the same task by registering `RateLimiter::for('security-csp-report', fn ($request) => Limit::perMinute(10)->by('security-csp-report|'.$request->ip()))` in `app/Providers/AppServiceProvider.php`, keeping the exact `/csp/report` route public and CSRF-exempt in `routes/web.php` while adding `->middleware('throttle:security-csp-report')` before `->name('security.csp.report')`, augmenting the metadata payload passed into `recordViolation()` in `app/Http/Controllers/CspViolationReportController.php` with the exact pair `'source' => 'csp-report'`, adding `MassPrunable` plus a `prunable()` query in `app/Models/SecurityViolation.php` that targets only rows where `type` is `SecurityViolationType::DATA_ACCESS`, `metadata->source` is `csp-report`, and `occurred_at` is at least 14 days old, and scheduling `Schedule::command('model:prune', ['--model' => [SecurityViolation::class]])->daily();` in `routes/console.php`. Preserve the current `202` response and existing `SecurityViolationDetected` flow, and do not widen pruning to every security violation row.</action>
  <acceptance_criteria>
    - `tests/Feature/Security/CspReportRateLimitTest.php` exists and contains `assertTooManyRequests`.
    - `tests/Feature/Security/CspReportRateLimitTest.php` contains `csp-report`.
    - `tests/Feature/Security/SecurityHeadersTest.php` contains `'source' => 'csp-report'`.
    - `app/Providers/AppServiceProvider.php` contains `RateLimiter::for('security-csp-report'`.
    - `routes/web.php` contains `middleware('throttle:security-csp-report')`.
    - `app/Http/Controllers/CspViolationReportController.php` contains `'source' => 'csp-report'`.
    - `app/Models/SecurityViolation.php` contains `MassPrunable`.
    - `app/Models/SecurityViolation.php` contains `now()->subDays(14)`.
    - `routes/console.php` contains `Schedule::command('model:prune'`.
    - The automated verification command passes.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Security/SecurityHeadersTest.php tests/Feature/Security/CspReportRateLimitTest.php --compact</automated>
  </verify>
  <done>The public CSP endpoint keeps returning `202` for valid reports, the eleventh repeated request is throttled with `429`, CSP rows carry the `csp-report` source tag, old tagged rows are prunable after 14 days, and the suite passes green at the end of the task.</done>
</task>

</tasks>

<verification>
Run the CSP-specific feature suite and confirm accepted reports still return `202` while the eleventh repeat request returns `429`.
</verification>

<success_criteria>
- `/csp/report` remains public and returns `202` for valid reports.
- The route uses the `security-csp-report` limiter and enforces `429` after ten requests per minute per IP.
- `SecurityViolation` rows created from CSP reports carry `metadata['source'] = 'csp-report'` and are eligible for pruning after 14 days.
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-freeze-and-guardrails/01-03-SUMMARY.md`
</output>
