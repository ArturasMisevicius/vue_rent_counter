---
phase: 01-safety-freeze-and-guardrails
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - composer.lock
  - config/pwa.php
  - public/manifest.json
  - public/offline.html
  - public/sw.js
  - bootstrap/cache/packages.php
  - bootstrap/cache/services.php
  - tests/Feature/Public/PwaIntegrationTest.php
  - tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php
autonomous: true
requirements:
  - SEC-05
must_haves:
  truths:
    - "Rendered public, guest, and tenant pages emit no manifest tag or service-worker registration code."
    - "The repository ships no PWA package, config, manifest, offline page, or service worker."
    - "Regression tests fail if the PWA surface reappears."
  artifacts:
    - path: "composer.json"
      provides: "Dependency manifest with no `erag/laravel-pwa` requirement"
      contains: "\"require\""
    - path: "tests/Feature/Public/PwaIntegrationTest.php"
      provides: "Rendered-surface proof that PWA hooks are absent"
      contains: "assertDontSee('rel=\"manifest\"', false)"
    - path: "tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php"
      provides: "Inventory proof that package, config, and public assets are gone"
      contains: "erag/laravel-pwa"
  key_links:
    - from: "tests/Feature/Public/PwaIntegrationTest.php"
      to: "public, guest, and tenant pages"
      via: "HTML assertions for missing hooks"
      pattern: "assertDontSee\\('serviceWorker\\.register'"
    - from: "tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php"
      to: "composer.json and composer.lock"
      via: "package removal assertions"
      pattern: "erag/laravel-pwa"
---

<objective>
Remove the half-enabled PWA surface completely and replace the old presence checks with removal proof.

Purpose: Prevent hidden package auto-discovery and public assets from leaving a stray public surface in the product.
Output: No PWA dependency, config, or public assets plus tests that prove rendered pages emit no PWA hooks.
</objective>

<execution_context>
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
@.planning/phases/01-safety-freeze-and-guardrails/01-RESEARCH.md
@composer.json
@config/pwa.php
@public/manifest.json
@public/offline.html
@public/sw.js
@tests/Feature/Public/PwaIntegrationTest.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace PWA-presence tests with removal inventory proof</name>
  <files>tests/Feature/Public/PwaIntegrationTest.php, tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php</files>
  <read_first>
    - tests/Feature/Public/PwaIntegrationTest.php
    - composer.json
    - composer.lock
    - config/pwa.php
    - public/manifest.json
    - public/offline.html
    - public/sw.js
    - .planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: The home page, login page, and tenant dashboard no longer contain `rel="manifest"` or `serviceWorker.register`.
    - Test 2: `composer.json` and `composer.lock` no longer contain `erag/laravel-pwa`.
    - Test 3: `config/pwa.php`, `public/manifest.json`, `public/offline.html`, and `public/sw.js` are absent.
    - Test 4: `GET /manifest.json` and `GET /sw.js` return `404` without relying on `tests/Feature/Security/NoPublicDebugFilesTest.php`.
  </behavior>
  <action>Rewrite `tests/Feature/Public/PwaIntegrationTest.php` so each rendered-surface assertion uses `assertDontSee('rel=\"manifest\"', false)` and `assertDontSee('serviceWorker.register', false)`. Add `tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php` that reads `composer.json` and `composer.lock` to assert the exact string `erag/laravel-pwa` is gone, asserts `config/pwa.php`, `public/manifest.json`, `public/offline.html`, and `public/sw.js` do not exist, and makes direct HTTP assertions that `GET /manifest.json` and `GET /sw.js` both return `404`. Keep the absence proof self-contained inside these PWA-specific tests; do not rely on `tests/Feature/Security/NoPublicDebugFilesTest.php` for any manifest or service-worker coverage.</action>
  <acceptance_criteria>
    - `tests/Feature/Public/PwaIntegrationTest.php` contains `assertDontSee('rel=\"manifest\"', false)`.
    - `tests/Feature/Public/PwaIntegrationTest.php` contains `assertDontSee('serviceWorker.register', false)`.
    - `tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php` exists and contains `erag/laravel-pwa`.
    - `tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php` contains `public/sw.js`.
    - `tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php` contains `/manifest.json` and `/sw.js`.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Public/PwaIntegrationTest.php tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php --compact</automated>
  </verify>
  <done>The repository has failing-first tests that require the PWA dependency, config, public assets, and rendered hooks to be absent.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the PWA dependency, config, and public assets without stubs</name>
  <files>composer.json, composer.lock, config/pwa.php, public/manifest.json, public/offline.html, public/sw.js, bootstrap/cache/packages.php, bootstrap/cache/services.php</files>
  <read_first>
    - composer.json
    - composer.lock
    - config/pwa.php
    - public/manifest.json
    - public/offline.html
    - public/sw.js
    - tests/Feature/Public/PwaIntegrationTest.php
    - tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php
    - .planning/phases/01-safety-freeze-and-guardrails/01-RESEARCH.md
  </read_first>
  <action>Run `composer remove erag/laravel-pwa --no-interaction` so both `composer.json` and `composer.lock` drop the package and the tracked package-discovery cache files refresh. Delete `config/pwa.php`, `public/manifest.json`, `public/offline.html`, and `public/sw.js`. Do not replace the removed surface with placeholder manifest files, dormant service-worker scripts, or view-level feature flags; the target state is full removal, not a disabled shell.</action>
  <acceptance_criteria>
    - `composer.json` does not contain `erag/laravel-pwa`.
    - `composer.lock` does not contain `erag/laravel-pwa`.
    - `config/pwa.php` is absent from the working tree.
    - `public/manifest.json`, `public/offline.html`, and `public/sw.js` are absent from the working tree.
    - The automated verification command passes.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Public/PwaIntegrationTest.php tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php --compact</automated>
  </verify>
  <done>The PWA package, config, and public assets are fully removed, and the rendered application emits no PWA hooks.</done>
</task>

</tasks>

<verification>
Run the PWA removal suite and confirm both rendered pages and repository inventory stay free of package/config/public traces.
</verification>

<success_criteria>
- `composer.json` and `composer.lock` no longer reference `erag/laravel-pwa`.
- `config/pwa.php`, `public/manifest.json`, `public/offline.html`, and `public/sw.js` are gone.
- The public, guest, and tenant shell tests confirm there is no manifest or service-worker registration markup.
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-freeze-and-guardrails/01-02-SUMMARY.md`
</output>
