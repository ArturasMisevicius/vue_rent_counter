---
phase: 01-safety-freeze-and-guardrails
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/web.php
  - routes/testing.php
  - tests/Feature/Security/NoPublicDebugFilesTest.php
  - tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php
autonomous: true
requirements:
  - SEC-05
must_haves:
  truths:
    - "The live application route graph exposes no `__test/*` endpoints."
    - "Direct requests to legacy public debug paths return `404`."
    - "`public/index.php` remains the only executable PHP entrypoint in `public/`."
    - "Feature tests still have access to named helper routes through the Pest bootstrap only."
  artifacts:
    - path: "routes/web.php"
      provides: "Public route graph without a live include of test-only routes"
      contains: "Route::post('/csp/report', CspViolationReportController::class)"
    - path: "tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php"
      provides: "Executable proof that the normal app route graph excludes `__test/*` routes"
      contains: "route:list --path=__test --json"
    - path: "tests/Feature/Security/NoPublicDebugFilesTest.php"
      provides: "Direct-request regression coverage for removed public debug and test paths"
      contains: "/__test/intended"
    - path: "tests/Feature/Security/NoPublicDebugFilesTest.php"
      provides: "Filesystem regression proof that `public/index.php` stays the only public PHP entrypoint"
      contains: "glob(public_path('*.php'))"
  key_links:
    - from: "tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php"
      to: "routes/web.php"
      via: "CLI route inventory assertion"
      pattern: "route:list --path=__test --json"
    - from: "tests/Pest.php"
      to: "named helper routes"
      via: "registerSharedTestRoutes()"
      pattern: "Route::middleware\\("
    - from: "tests/Feature/Security/NoPublicDebugFilesTest.php"
      to: "public PHP inventory"
      via: "filesystem assertion"
      pattern: "index.php"
---

<objective>
Remove test-only routes from the live public route graph and add executable proof that they stay out.

Purpose: Lock down unsafe public test exposure before deeper modernization starts.
Output: A public route graph with no live `__test/*` routes plus regression tests that fail if those routes reappear.
</objective>

<execution_context>
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
@.planning/phases/01-safety-freeze-and-guardrails/01-RESEARCH.md
@.planning/phases/01-safety-freeze-and-guardrails/01-VALIDATION.md
@routes/web.php
@routes/testing.php
@tests/Pest.php
@tests/Feature/Security/NoPublicDebugFilesTest.php

<interfaces>
From tests/Pest.php:
```php
function registerSharedTestRoutes(): void
{
    if (! Route::has('test.intended')) {
        Route::middleware(['web', 'auth'])
            ->get('/__test/intended', fn () => 'intended')
            ->name('test.intended');
    }
    // Additional __test routes follow the same pattern.
}
```

From routes/web.php:
```php
Route::post('/csp/report', CspViolationReportController::class)
    ->withoutMiddleware([VerifyCsrfToken::class])
    ->name('security.csp.report');

require base_path('routes/testing.php');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public-surface regression proof and remove the live test-route include</name>
  <files>routes/web.php, routes/testing.php, tests/Feature/Security/NoPublicDebugFilesTest.php, tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php</files>
  <read_first>
    - tests/Feature/Security/NoPublicDebugFilesTest.php
    - tests/Pest.php
    - routes/web.php
    - routes/testing.php
    - .planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
    - .planning/phases/01-safety-freeze-and-guardrails/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: The legacy public debug entrypoints covered by `tests/Feature/Security/NoPublicDebugFilesTest.php` still return `404`.
    - Test 2: `php artisan route:list --path=__test --json` returns `[]` once the live route include is removed.
    - Test 3: `routes/web.php` no longer contains `require base_path('routes/testing.php');`.
    - Test 4: Pest-bootstrap helper routes still resolve inside feature tests, for example `route('test.intended')` resolves to `/__test/intended` and remains protected by the route middleware stack.
    - Test 5: `tests/Feature/Security/NoPublicDebugFilesTest.php` still proves `glob(public_path('*.php'))` resolves to `['index.php']` only.
  </behavior>
  <action>Start with the red step by creating `tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php` and keeping `tests/Feature/Security/NoPublicDebugFilesTest.php` focused on the legacy public debug entrypoints plus `/test-debug` while retaining its existing filesystem proof that `public/index.php` is the only PHP file under `public/`. The suite must demand five exact outcomes: `routes/web.php` does not contain `require base_path('routes/testing.php');`, an external `php artisan route:list --path=__test --json` process returns `[]`, the legacy public debug endpoints covered by `NoPublicDebugFilesTest.php` still return `404`, `NoPublicDebugFilesTest.php` still asserts `glob(public_path('*.php'))` resolves to `['index.php']`, and Pest-bootstrap helper routes still resolve in test context by asserting `route('test.intended')` returns a path ending in `/__test/intended` and a guest request to that resolved route is redirected by the auth middleware instead of 404ing. Complete the green step in the same task by deleting the exact line `require base_path('routes/testing.php');` from `routes/web.php` and removing `routes/testing.php` from the repository. Do not replace the deleted include with `app()->runningUnitTests()` checks, environment guards, or any other public-route shim under `routes/`; the only shared definitions for `test.intended`, `test.session-timeout.web`, `test.security.secure-page`, `test.errors.forbidden`, and `test.errors.server` must remain in `tests/Pest.php` and file-local test setup helpers so this task's automated verification command passes at the end.</action>
  <acceptance_criteria>
    - `tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php` exists and contains `route:list --path=__test --json`.
    - `tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php` contains the exact string `routes/testing.php`.
    - `tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php` contains `route('test.intended')`.
    - `tests/Feature/Security/NoPublicDebugFilesTest.php` contains `/test-debug`.
    - `tests/Feature/Security/NoPublicDebugFilesTest.php` contains `glob(public_path('*.php'))`.
    - `tests/Feature/Security/NoPublicDebugFilesTest.php` contains `['index.php']`.
    - `routes/web.php` does not contain `routes/testing.php`.
    - `routes/testing.php` is absent from the working tree.
    - The automated verification command passes.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Security/NoPublicDebugFilesTest.php tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php --compact</automated>
  </verify>
  <done>The live public route graph no longer includes test-only routes, legacy public debug entrypoints still return `404`, `public/index.php` remains the only public PHP entrypoint, Pest helper routes remain available only through the test bootstrap, and the proof suite passes green at the end of the task.</done>
</task>

</tasks>

<verification>
Run the public-surface proof suite and confirm it stays green after the route include is removed.
</verification>

<success_criteria>
- `php artisan route:list --path=__test --json` returns `[]` in an external process.
- Direct requests to legacy public debug paths return `404`.
- `glob(public_path('*.php'))` resolves to `['index.php']`.
- Pest helper routes still resolve only through the test bootstrap.
- The repository no longer contains `routes/testing.php`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-freeze-and-guardrails/01-01-SUMMARY.md`
</output>
