---
phase: 01-safety-freeze-and-guardrails
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/web.php
  - routes/testing.php
  - tests/Feature/Security/NoPublicDebugFilesTest.php
  - tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php
autonomous: true
requirements:
  - SEC-05
must_haves:
  truths:
    - "The live application route graph exposes no `__test/*` endpoints."
    - "Direct requests to legacy debug paths and `__test/*` paths return `404`."
    - "Feature tests still have access to named helper routes through the Pest bootstrap only."
  artifacts:
    - path: "routes/web.php"
      provides: "Public route graph without a live include of test-only routes"
      contains: "Route::post('/csp/report', CspViolationReportController::class)"
    - path: "tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php"
      provides: "Executable proof that the normal app route graph excludes `__test/*` routes"
      contains: "route:list --path=__test --json"
    - path: "tests/Feature/Security/NoPublicDebugFilesTest.php"
      provides: "Direct-request regression coverage for removed public debug and test paths"
      contains: "/__test/intended"
  key_links:
    - from: "tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php"
      to: "routes/web.php"
      via: "CLI route inventory assertion"
      pattern: "route:list --path=__test --json"
    - from: "tests/Pest.php"
      to: "named helper routes"
      via: "registerSharedTestRoutes()"
      pattern: "Route::middleware\\("
---

<objective>
Remove test-only routes from the live public route graph and add executable proof that they stay out.

Purpose: Lock down unsafe public test exposure before deeper modernization starts.
Output: A public route graph with no live `__test/*` routes plus regression tests that fail if those routes reappear.
</objective>

<execution_context>
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
@.planning/phases/01-safety-freeze-and-guardrails/01-RESEARCH.md
@.planning/phases/01-safety-freeze-and-guardrails/01-VALIDATION.md
@routes/web.php
@routes/testing.php
@tests/Pest.php
@tests/Feature/Security/NoPublicDebugFilesTest.php

<interfaces>
From tests/Pest.php:
```php
function registerSharedTestRoutes(): void
{
    if (! Route::has('test.intended')) {
        Route::middleware(['web', 'auth'])
            ->get('/__test/intended', fn () => 'intended')
            ->name('test.intended');
    }
    // Additional __test routes follow the same pattern.
}
```

From routes/web.php:
```php
Route::post('/csp/report', CspViolationReportController::class)
    ->withoutMiddleware([VerifyCsrfToken::class])
    ->name('security.csp.report');

require base_path('routes/testing.php');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public-surface regression proof for removed test routes</name>
  <files>tests/Feature/Security/NoPublicDebugFilesTest.php, tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php</files>
  <read_first>
    - tests/Feature/Security/NoPublicDebugFilesTest.php
    - tests/Pest.php
    - routes/web.php
    - routes/testing.php
    - .planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
    - .planning/phases/01-safety-freeze-and-guardrails/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `GET /__test/intended`, `GET /__test/session-timeout`, `GET /__test/security/secure-page`, `GET /__test/errors/forbidden`, and `GET /__test/errors/server` return `404` from the normal application surface.
    - Test 2: `php artisan route:list --path=__test --json` returns `[]` once the live route include is removed.
    - Test 3: `routes/web.php` no longer contains `require base_path('routes/testing.php');`.
  </behavior>
  <action>Create `tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php` with two executable assertions: first, `routes/web.php` does not contain the exact string `require base_path('routes/testing.php');`; second, an external `php artisan route:list --path=__test --json` process returns an empty JSON array. Expand `tests/Feature/Security/NoPublicDebugFilesTest.php` so its dataset also covers `/__test/intended`, `/__test/session-timeout`, `/__test/security/secure-page`, `/__test/errors/forbidden`, and `/__test/errors/server`. Keep the assertions concrete: `assertNotFound()` for direct requests and an exact empty-array expectation for the route inventory command.</action>
  <acceptance_criteria>
    - `tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php` exists and contains `route:list --path=__test --json`.
    - `tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php` contains the exact string `routes/testing.php`.
    - `tests/Feature/Security/NoPublicDebugFilesTest.php` contains `/__test/intended` and `/__test/security/secure-page`.
    - The automated verification command passes.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Security/NoPublicDebugFilesTest.php tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php --compact</automated>
  </verify>
  <done>The repository has failing-first regression proof that the normal app must not expose `__test/*` routes or keep the live `routes/testing.php` include.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the live `routes/testing.php` route graph include</name>
  <files>routes/web.php, routes/testing.php</files>
  <read_first>
    - routes/web.php
    - routes/testing.php
    - tests/Pest.php
    - tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php
    - tests/Feature/Security/NoPublicDebugFilesTest.php
    - .planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
  </read_first>
  <action>Delete the exact line `require base_path('routes/testing.php');` from `routes/web.php`. Remove `routes/testing.php` from the repository instead of leaving an unused placeholder file behind. Do not replace the deleted include with `app()->runningUnitTests()` checks, environment guards, or any other public-route shim under `routes/`; the only shared definitions for `test.intended`, `test.session-timeout.web`, `test.security.secure-page`, `test.errors.forbidden`, and `test.errors.server` must remain in `tests/Pest.php` and file-local test setup helpers.</action>
  <acceptance_criteria>
    - `routes/web.php` does not contain `routes/testing.php`.
    - `routes/testing.php` is absent from the working tree.
    - `tests/Pest.php` still contains `registerSharedTestRoutes`.
    - The automated verification command passes.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Security/NoPublicDebugFilesTest.php tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php --compact</automated>
  </verify>
  <done>The live public route graph no longer includes test-only routes, and the only remaining shared `__test/*` route definitions live in the Pest bootstrap for test runs.</done>
</task>

</tasks>

<verification>
Run the public-surface proof suite and confirm it stays green after the route include is removed.
</verification>

<success_criteria>
- `php artisan route:list --path=__test --json` returns `[]` in an external process.
- Direct requests to legacy debug and `__test/*` paths return `404`.
- The repository no longer contains `routes/testing.php`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-freeze-and-guardrails/01-01-SUMMARY.md`
</output>
