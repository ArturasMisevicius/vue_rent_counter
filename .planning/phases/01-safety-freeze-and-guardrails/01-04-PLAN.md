---
phase: 01-safety-freeze-and-guardrails
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - composer.json
  - tests/Feature/Admin/FilamentCrudCoverageInventoryTest.php
  - .github/workflows/phase-1-guardrails.yml
autonomous: true
requirements:
  - GOV-03
  - OPS-04
must_haves:
  truths:
    - "Maintainers can run one shared command that fails when Phase 1 formatting or guarded regressions fail."
    - "GitHub Actions runs that same shared command for pull requests and pushes to `main`."
    - "The shared guard command uses executable architecture and inventory guard tests as the Phase 1 static-check layer without adding PHPStan or Larastan in this phase."
    - "The shared guard command explicitly proves OPS-04 coverage by running tenant-isolation, role-bound access, and billing-invariant regression tests in the same curated bundle."
  artifacts:
    - path: "composer.json"
      provides: "Shared `guard:phase1` command that includes Pint plus the architecture/inventory static-check layer"
      contains: "tests/Feature/Architecture/FilamentFoundationPlacementTest.php"
    - path: "composer.json"
      provides: "Shared `guard:phase1` command that proves tenant and tenant-portal isolation as part of the curated OPS-04 regression bundle"
      contains: "tests/Feature/Security/TenantIsolationTest.php"
    - path: "composer.json"
      provides: "Shared `guard:phase1` command that proves role-bound superadmin surface access as part of the curated OPS-04 regression bundle"
      contains: "tests/Feature/Filament/SuperadminResourcesTest.php"
    - path: "composer.json"
      provides: "Shared `guard:phase1` command that proves billing invariants stay protected during modernization"
      contains: "tests/Feature/Admin/InvoiceImmutabilityTest.php"
    - path: ".github/workflows/phase-1-guardrails.yml"
      provides: "Merge-time workflow that calls the shared guard command"
      contains: "composer guard:phase1"
    - path: "tests/Feature/Admin/FilamentCrudCoverageInventoryTest.php"
      provides: "Pint-clean inventory guard included in the curated Phase 1 static-check layer"
      contains: "App\\Filament\\Resources\\AuditLogs\\AuditLogResource"
    - path: "tests/Feature/Architecture/FilamentFoundationPlacementTest.php"
      provides: "Executable architecture guard included in the curated Phase 1 static-check layer"
      contains: "App\\Filament\\Actions"
  key_links:
    - from: ".github/workflows/phase-1-guardrails.yml"
      to: "composer.json"
      via: "shared command invocation"
      pattern: "composer guard:phase1"
    - from: "composer.json"
      to: "Phase 1 static-check and regression files"
      via: "explicit Pest file list"
      pattern: "tests/Feature/Architecture/FilamentFoundationPlacementTest.php"
    - from: "composer.json"
      to: "OPS-04 tenant and billing regression files"
      via: "explicit Pest file list"
      pattern: "tests/Feature/Security/TenantIsolationTest.php"
    - from: "composer.json"
      to: "OPS-04 role-bound access regression files"
      via: "explicit Pest file list"
      pattern: "tests/Feature/Filament/SuperadminResourcesTest.php"
---

<objective>
Add one shared local and CI guard command that blocks Phase 1 modernization work when formatting or the curated regression bundle fails.

Purpose: Turn the verified Phase 1 safety checks into an unavoidable merge-time gate.
Output: A `composer guard:phase1` entrypoint, an explicit architecture/inventory static-check layer inside that command, a GitHub Actions workflow that reuses it, and a clean baseline for the current in-scope Pint target.
</objective>

<execution_context>
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
@.planning/phases/01-safety-freeze-and-guardrails/01-RESEARCH.md
@.planning/phases/01-safety-freeze-and-guardrails/01-VALIDATION.md
@composer.json
@tests/Feature/Admin/FilamentCrudCoverageInventoryTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the shared `guard:phase1` command and clear the current Pint baseline</name>
  <files>composer.json, tests/Feature/Admin/FilamentCrudCoverageInventoryTest.php</files>
  <read_first>
    - composer.json
    - tests/Feature/Admin/FilamentCrudCoverageInventoryTest.php
    - .planning/phases/01-safety-freeze-and-guardrails/01-VALIDATION.md
    - .planning/phases/01-safety-freeze-and-guardrails/01-RESEARCH.md
    - tests/Feature/Security/NoPublicDebugFilesTest.php
    - tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php
    - tests/Feature/Public/PwaIntegrationTest.php
    - tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php
    - tests/Feature/Security/SecurityHeadersTest.php
    - tests/Feature/Security/CspReportRateLimitTest.php
    - tests/Feature/Architecture/FilamentFoundationPlacementTest.php
    - tests/Feature/Admin/FilamentCrudCoverageInventoryTest.php
  </read_first>
  <action>Reorder the imports in `tests/Feature/Admin/FilamentCrudCoverageInventoryTest.php` so the path-scoped Pint command is clean. In `composer.json`, add a script named `guard:phase1` with these exact steps in order: `@php artisan config:clear --ansi`; `vendor/bin/pint --test app config database routes tests resources/views bootstrap/app.php`; `@php artisan test tests/Feature/Security/NoPublicDebugFilesTest.php tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php tests/Feature/Public/PwaIntegrationTest.php tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php tests/Feature/Security/SecurityHeadersTest.php tests/Feature/Security/CspReportRateLimitTest.php tests/Feature/Security/TenantIsolationTest.php tests/Feature/Security/TenantPortalIsolationTest.php tests/Feature/Filament/SuperadminResourcesTest.php tests/Feature/Architecture/FilamentFoundationPlacementTest.php tests/Feature/Admin/FilamentCrudCoverageInventoryTest.php tests/Feature/Admin/InvoiceImmutabilityTest.php tests/Feature/Admin/TenantUnassignmentInvoiceRetentionTest.php --compact`. Treat `tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php`, `tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php`, `tests/Feature/Architecture/FilamentFoundationPlacementTest.php`, and `tests/Feature/Admin/FilamentCrudCoverageInventoryTest.php` as the explicit Phase 1 static-check layer that satisfies GOV-03 for this phase instead of bootstrapping PHPStan or Larastan. Do not use `vendor/bin/pint --test` without explicit paths; it currently drags `_old/` and tracked cache files into the gate.</action>
  <acceptance_criteria>
    - `composer.json` contains `"guard:phase1"`.
    - `composer.json` contains `vendor/bin/pint --test app config database routes tests resources/views bootstrap/app.php`.
    - `composer.json` contains `tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php`.
    - `composer.json` contains `tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php`.
    - `composer.json` contains `tests/Feature/Security/TenantIsolationTest.php`.
    - `composer.json` contains `tests/Feature/Security/TenantPortalIsolationTest.php`.
    - `composer.json` contains `tests/Feature/Filament/SuperadminResourcesTest.php`.
    - `composer.json` contains `tests/Feature/Architecture/FilamentFoundationPlacementTest.php`.
    - `composer.json` contains `tests/Feature/Security/CspReportRateLimitTest.php`.
    - `composer.json` contains `tests/Feature/Admin/FilamentCrudCoverageInventoryTest.php`.
    - `composer.json` contains `tests/Feature/Admin/InvoiceImmutabilityTest.php`.
    - `composer.json` contains `tests/Feature/Admin/TenantUnassignmentInvoiceRetentionTest.php`.
    - The automated verification command passes.
  </acceptance_criteria>
  <verify>
    <automated>rg 'tests/Feature/Security/TenantIsolationTest.php' composer.json && rg 'tests/Feature/Security/TenantPortalIsolationTest.php' composer.json && rg 'tests/Feature/Filament/SuperadminResourcesTest.php' composer.json && rg 'tests/Feature/Admin/InvoiceImmutabilityTest.php' composer.json && rg 'tests/Feature/Admin/TenantUnassignmentInvoiceRetentionTest.php' composer.json && vendor/bin/pint --test app config database routes tests resources/views bootstrap/app.php && php artisan test tests/Feature/Architecture/Phase1PublicSurfaceInventoryTest.php tests/Feature/Architecture/PwaSurfaceRemovalInventoryTest.php tests/Feature/Architecture/FilamentFoundationPlacementTest.php tests/Feature/Admin/FilamentCrudCoverageInventoryTest.php --compact</automated>
  </verify>
  <done>Maintainers can run one local command that clears config, runs a source-only Pint check, and executes a curated guard bundle whose static-check layer is the four executable architecture/inventory guard tests and whose regression list explicitly includes the tenant, role, and billing OPS-04 files instead of PHPStan.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Phase 1 GitHub Actions guard workflow that reuses the shared command</name>
  <files>.github/workflows/phase-1-guardrails.yml</files>
  <read_first>
    - composer.json
    - .planning/phases/01-safety-freeze-and-guardrails/01-RESEARCH.md
    - .planning/phases/01-safety-freeze-and-guardrails/01-VALIDATION.md
    - .env.example
  </read_first>
  <action>Create `.github/workflows/phase-1-guardrails.yml` with the exact workflow name `Phase 1 Guardrails`. Trigger it on `pull_request` and `push` to `main`. Use `actions/checkout@v6`, `shivammathur/setup-php@v2` with `php-version: '8.5'`, and `actions/cache@v5` for the Composer cache directory. After `composer install --no-interaction --prefer-dist --no-progress`, copy `.env.example` to `.env` when needed, run `php artisan key:generate --ansi`, and then run `composer guard:phase1`. The workflow must rely on the shared command so the same Phase 1 static-check layer travels with it: the path-scoped Pint run plus the architecture/inventory guard tests `Phase1PublicSurfaceInventoryTest.php`, `PwaSurfaceRemovalInventoryTest.php`, `FilamentFoundationPlacementTest.php`, and `FilamentCrudCoverageInventoryTest.php`, alongside the rest of the curated regression bundle. Do not duplicate the explicit Pint or Pest file list inside the workflow YAML; the workflow must call the shared Composer command verbatim.</action>
  <acceptance_criteria>
    - `.github/workflows/phase-1-guardrails.yml` exists.
    - `.github/workflows/phase-1-guardrails.yml` contains `name: Phase 1 Guardrails`.
    - `.github/workflows/phase-1-guardrails.yml` contains `shivammathur/setup-php@v2`.
    - `.github/workflows/phase-1-guardrails.yml` contains `composer guard:phase1`.
    - `.github/workflows/phase-1-guardrails.yml` contains `php artisan key:generate --ansi`.
  </acceptance_criteria>
  <verify>
    <automated>composer guard:phase1</automated>
  </verify>
  <done>The repository contains a merge-time workflow that provisions PHP 8.5 and executes the exact same Phase 1 guard command used locally.</done>
</task>

</tasks>

<verification>
Run the fast Task 1 verification command before `composer guard:phase1` exists, then run `composer guard:phase1` after the first two tasks.
</verification>

<success_criteria>
- `composer guard:phase1` exists and fails when formatting or any guarded regression in the explicit file list fails.
- `.github/workflows/phase-1-guardrails.yml` runs `composer guard:phase1` instead of duplicating command logic.
- The Phase 1 static-check layer inside `composer guard:phase1` is `Phase1PublicSurfaceInventoryTest.php`, `PwaSurfaceRemovalInventoryTest.php`, `FilamentFoundationPlacementTest.php`, and `FilamentCrudCoverageInventoryTest.php`, not PHPStan bootstrap.
- The shared guard bundle also includes explicit tenant-isolation, role-bound access, and billing-invariant regression files for OPS-04.
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-freeze-and-guardrails/01-04-SUMMARY.md`
</output>
