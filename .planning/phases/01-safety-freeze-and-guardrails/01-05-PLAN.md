---
phase: 01-safety-freeze-and-guardrails
plan: "05"
type: execute
wave: 4
depends_on:
  - "01-04"
files_modified:
  - .github/workflows/phase-1-guardrails.yml
autonomous: false
pause_resume_boundary:
  pause_after_task: "01"
  resume_before_task: "02"
  reason: "GitHub branch protection cannot require `Phase 1 Guardrails` until the workflow has been merged to remote `main` and completed successfully at least once."
requirements:
  - GOV-03
must_haves:
  truths:
    - "The `Phase 1 Guardrails` workflow exists on remote `main` and has completed successfully at least once before branch protection is configured."
    - "The default `main` branch cannot accept a merge while the `Phase 1 Guardrails` check is failing or missing."
    - "The branch-protection requirement is machine-verifiable with GitHub CLI when available or the GitHub REST API when `gh` is unavailable."
  artifacts:
    - path: ".github/workflows/phase-1-guardrails.yml"
      provides: "Exact workflow name that becomes the required remote status check"
      contains: "name: Phase 1 Guardrails"
  key_links:
    - from: ".github/workflows/phase-1-guardrails.yml"
      to: "ArturasMisevicius/vue_rent_counter main branch protection"
      via: "exact status-check name reused by remote enforcement"
      pattern: "Phase 1 Guardrails"
---

<objective>
Require the remote `main` branch to pass `Phase 1 Guardrails` before merge once the workflow exists on GitHub.

Purpose: Make the new guard workflow unavoidable for merges instead of leaving it as an advisory CI check.
Output: One successful remote `Phase 1 Guardrails` run on `main`, then branch protection that requires that exact check before merge.
</objective>

<execution_context>
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/andrejprus/Herd/tenanto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
@.planning/phases/01-safety-freeze-and-guardrails/01-RESEARCH.md
@.planning/phases/01-safety-freeze-and-guardrails/01-VALIDATION.md
@.github/workflows/phase-1-guardrails.yml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Wait for the first successful remote `Phase 1 Guardrails` run on `main`</name>
  <files>.github/workflows/phase-1-guardrails.yml</files>
  <read_first>
    - .planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
    - .planning/ROADMAP.md
    - .github/workflows/phase-1-guardrails.yml
  </read_first>
  <action>Merge the workflow change that adds `.github/workflows/phase-1-guardrails.yml` to the remote `main` branch for `ArturasMisevicius/vue_rent_counter`, then wait until the `Phase 1 Guardrails` workflow has completed successfully at least once on `main` so the status check exists remotely and can be required by branch protection.</action>
  <instructions>
    Complete the PR merge or equivalent remote update that puts `.github/workflows/phase-1-guardrails.yml` on `main`.
    Wait until GitHub shows one successful `Phase 1 Guardrails` run on `main`.
    This checkpoint exists because the required status check cannot be configured until the workflow has existed and run remotely at least once.
  </instructions>
  <verification>Remote GitHub Actions shows at least one successful `Phase 1 Guardrails` run on `main` for `ArturasMisevicius/vue_rent_counter`.</verification>
  <resume-signal>Type "done" after the workflow has run successfully on remote `main`</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Configure the default branch to require `Phase 1 Guardrails` before merge</name>
  <files>.github/workflows/phase-1-guardrails.yml</files>
  <read_first>
    - .planning/phases/01-safety-freeze-and-guardrails/01-CONTEXT.md
    - .planning/ROADMAP.md
    - .github/workflows/phase-1-guardrails.yml
  </read_first>
  <action>After Task 1 is complete, automate branch protection for `ArturasMisevicius/vue_rent_counter` on the `main` branch so the exact status check `Phase 1 Guardrails` is required before merge. Prefer GitHub CLI when available: use `gh api repos/ArturasMisevicius/vue_rent_counter/branches/main/protection --method PUT --input -` with a payload that enables required status checks, sets `strict` to `true`, and includes `[{\"context\":\"Phase 1 Guardrails\",\"app_id\":null}]` under `checks`. If `gh` is unavailable, use the GitHub REST API with `curl` and a token-bearing environment variable such as `GITHUB_TOKEN` to send the equivalent branch-protection payload. If execution hits a missing-auth or missing-tool gate, let the executor create a dynamic authentication checkpoint and retry after credentials or tooling are provided.</action>
  <acceptance_criteria>
    - The default branch protection rule lists `Phase 1 Guardrails` as a required status check.
    - A pull request with a failing `Phase 1 Guardrails` run is blocked from merging.
    - A pull request without a completed `Phase 1 Guardrails` run is blocked from merging.
    - A post-change API or CLI read of branch protection includes `Phase 1 Guardrails`.
  </acceptance_criteria>
  <verify>
    <automated>if command -v gh >/dev/null 2>&1; then gh api repos/ArturasMisevicius/vue_rent_counter/branches/main/protection --jq '.required_status_checks.checks[]?.context'; else curl -fsSL -H "Accept: application/vnd.github+json" -H "Authorization: Bearer ${GITHUB_TOKEN:?GITHUB_TOKEN is required when gh is unavailable}" https://api.github.com/repos/ArturasMisevicius/vue_rent_counter/branches/main/protection; fi | rg 'Phase 1 Guardrails'</automated>
  </verify>
  <done>`Phase 1 Guardrails` is enforced as a required status check on `main`, and the requirement is machine-verifiable through GitHub CLI or the REST API instead of being left as a manual-only chore.</done>
</task>

</tasks>

<verification>
After the workflow is merged and has one successful remote `main` run, resume from the blocking checkpoint, apply branch protection, and verify the exact required check through GitHub CLI or the REST API.
</verification>

<success_criteria>
- The `Phase 1 Guardrails` workflow has completed successfully at least once on remote `main`.
- The default branch requires the exact `Phase 1 Guardrails` status check before merge.
- Branch-protection verification works with GitHub CLI when available and has a documented REST fallback when `gh` is unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-freeze-and-guardrails/01-05-SUMMARY.md`
</output>
