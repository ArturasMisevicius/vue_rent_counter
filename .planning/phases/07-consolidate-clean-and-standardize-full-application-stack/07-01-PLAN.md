---
phase: 07-consolidate-clean-and-standardize-full-application-stack
plan: "01"
type: execute
wave: 1
depends_on:
  - 06-01-PLAN.md
files_modified:
  - app/Http/Controllers
  - app/Livewire
  - app/Filament
  - app/Services
  - app/Actions
  - app/Filament/Actions
  - app/Http/Requests
  - public/*
  - tests
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - SEC-03
  - GOV-01
  - OPS-01
---

<objective>
Consolidate the full application surface by removing inline policy/validation drift, eliminating public debug entrypoints, and standardizing controller, request, Livewire, and Filament data flows to a shared architecture template before further billing specialization.
</objective>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/requirements/REQ-ARCH-01.md
@.planning/requirements/REQ-SEC-03.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the external surface and remove remaining debug/public entrypoints</name>
  <files>public/test-debug.php, public/translation-test.php, public/swap.php, resources/views/*
</files>
  <action>Create/extend a safety test that proves only `public/index.php` remains routable from public and remove or quarantine leftover debug/public bootstrap files. Confirm tenant and auth routes retain coverage and no `phpinfo`-style or ad hoc diagnostics are reachable in production profiles.</action>
  <verify>
    <automated>php artisan test tests/Feature/PublicSurfaceLockdownTest.php --compact</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consolidate controller architecture and role-scoped request validation</name>
  <files>app/Http/Controllers/Admin/*, app/Http/Controllers/Manager/*, app/Http/Controllers/Superadmin/*, app/Http/Controllers/Tenant/*, app/Http/Requests/*
</files>
  <action>Rehome inline controller validation into role-prefixed `FormRequest` classes, standardize role/tenant guards at the boundary, and route mutating flows through dedicated `app/Actions`/`app/Services` entrypoints. Preserve role contracts already defined (`SUPERADMIN`, `ADMIN`, `MANAGER`, `TENANT`).</action>
  <verify>
    <automated>php artisan test tests/Feature/Controllers/**/ --compact</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Standardize Livewire and Filament request/read/query contracts</name>
  <files>app/Livewire/*, app/Filament/**/*, app/Filament/Support/**/*, app/Filament/Resources/*
</files>
  <action>Replace inline validation logic in Livewire components with shared request-based validators, refactor duplicate read/builders into reusable support classes, and enforce eager-load/scoped read contracts on table and query-heavy screens to match role-scoped workspace boundaries.</action>
  <verify>
    <automated>php artisan test tests/Feature/Filament/** tests/Feature/Tenant/** --compact</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Normalize billing and shared services without duplicate orchestrators</name>
  <files>app/Services/BillingService.php, app/Services/*Billing*Service*.php, app/Actions/**/*, app/Support/**/*, tests/Feature/Billing/**
</files>
  <action>Unify billing orchestration entrypoints into a single domain service boundary, remove duplicated flow control in existing BillingService variants, and ensure invoice status/rules, rounding, and tenant workspace scoping are applied consistently from one canonical service path.</action>
  <verify>
    <automated>php artisan test tests/Feature/Billing/** tests/Feature/Architecture/RoleBoundaryTest.php --compact</automated>
  </verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/07-consolidate-clean-and-standardize-full-application-stack/07-01-SUMMARY.md`
</output>
