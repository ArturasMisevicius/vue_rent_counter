#!/bin/sh

set -eu

# Git does not include index changes made this late in the commit lifecycle.
# Keep changelog mutation in pre-commit so the staged entry lands in the
# current commit instead of being left behind for the next one.
exit 0
