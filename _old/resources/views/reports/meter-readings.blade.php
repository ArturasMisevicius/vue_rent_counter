<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <title>{{ __('reports.public.links.meter_readings') ?? 'Meter Readings' }}</title>
</head>
<body>
    <h1>{{ __('reports.public.links.meter_readings') ?? 'Meter Readings' }}</h1>
</body>
</html>
