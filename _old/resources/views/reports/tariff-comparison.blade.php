<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <title>{{ __('reports.public.links.tariff_comparison') ?? 'Tariff Comparison' }}</title>
</head>
<body>
    <h1>{{ __('reports.public.links.tariff_comparison') ?? 'Tariff Comparison' }}</h1>
</body>
</html>
